'use client';

const AsideMenu = (props: any) => {
	return (
		<div className={`flex flex-col bg-gray-200 text-gray-900`}>
			<div className={`h-20 w-20 bg-gradient-to-r from-indigo-500 to-blue-800 flex flex-col items-center justify-center text-gray-100`}>
                CV
            </div>
		</div>
	)
}

export default AsideMenu;